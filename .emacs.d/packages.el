(require 'key-chord)
(key-chord-mode 1)

(projectile-global-mode)

(global-evil-leader-mode)

(require 'evil)
(evil-mode 1)

(require 'auto-complete)
(require 'auto-complete-config)
(ac-config-default)
